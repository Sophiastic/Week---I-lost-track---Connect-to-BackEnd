/*import { Button } from "react-bootstrap";

type DeleteInstructionsProps = {
  id: number;
  onDeleteInstructions: (id: number) => void;
};

export default function DeleteInstructions({
  id,
  onDeleteInstructions,
}: DeleteInstructionsProps) {
  const handleDeleteInstructionsID = () => {
    onDeleteInstructions(id);
  };
  return (
    <>
      <Button
        variant="btn"
        style={{
          backgroundImage: "url('./assets/trash-can-solid.svg')",
          backgroundSize: "cover",
        }}
        onClick={handleDeleteInstructionsID}
      ></Button>
    </>
  );
}*/
